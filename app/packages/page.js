import Link from "next/link";
import { destinations } from "@/lib/destinations";
export const metadata = { title: "Packages", description: "Compare Travelogue destination packages, durations, starting prices, and itineraries." };
export default function PackagesPage(){return <section className="mx-auto max-w-6xl px-6 py-12 md:py-20"><p className="text-xs font-semibold text-mint tracking-wide mb-4">PACKAGES</p><h1 className="font-display text-4xl md:text-6xl mb-6">Every destination, priced.</h1><p className="text-slateSoft max-w-2xl mb-12">Starting points for considered journeys. Every itinerary can be tailored around your dates and pace.</p><div className="border-t border-white/10">{destinations.map(d=><div key={d.slug} className="py-6 border-b border-white/10 flex flex-col sm:flex-row sm:items-center justify-between gap-5"><div><Link href={`/destinations/${d.slug}`} className="text-lg font-semibold hover:text-mint">{d.name}, {d.country}</Link><p className="text-sm text-slateSoft mt-1">{d.tagline}</p></div><div className="flex items-center gap-5"><span className="text-sm text-slateSoft">{d.duration}</span><span className="font-semibold">${d.price.toLocaleString()}</span><Link href={`/destinations/${d.slug}`} className="rounded-full border border-white/15 px-4 py-2 text-xs font-semibold hover:border-mint hover:text-mint">View</Link></div></div>)}</div></section>}
