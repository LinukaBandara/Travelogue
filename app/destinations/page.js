import Image from "next/image";
import Link from "next/link";
import { destinations } from "@/lib/destinations";
export const metadata = { title: "Destinations", description: "Explore Travelogue's curated destinations across Asia." };
export default function DestinationsPage(){return <section className="mx-auto max-w-6xl px-6 py-12 md:py-20"><p className="text-xs font-semibold text-mint tracking-wide mb-4">DESTINATIONS</p><h1 className="font-display text-4xl md:text-6xl mb-6">Choose your next story.</h1><p className="text-slateSoft max-w-2xl mb-12">Seven destinations, each with a considered route, real itinerary, and one seamless request flow.</p><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">{destinations.map(d=><Link href={`/destinations/${d.slug}`} key={d.slug} className="group"><div className="relative aspect-[4/3] overflow-hidden rounded-2xl mb-4"><Image src={d.image} alt={`${d.name}, ${d.country}`} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" className="object-cover transition-transform duration-700 group-hover:scale-105"/></div><h2 className="font-display text-2xl group-hover:text-mint">{d.name}</h2><p className="text-sm text-slateSoft mt-1">{d.country} · {d.duration} · from ${d.price.toLocaleString()}</p></Link>)}</div></section>}
